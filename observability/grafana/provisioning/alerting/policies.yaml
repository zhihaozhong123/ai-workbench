# 智作台 - Grafana 告警路由策略
# 所有告警默认路由到 default 通知渠道（见 contact-points.yaml）。
apiVersion: 1
policies:
  - orgId: 1
    receiver: default
    group_by: ["alertname", "category"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes: []
