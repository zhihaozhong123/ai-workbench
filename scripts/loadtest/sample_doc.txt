小书童产品说明（压测示例文档）

小书童是一款面向个人与小微团队的知识库助手。用户可以把自己的文档（Word、PDF、TXT、Markdown 等）上传到知识库，系统会对文档做切片与向量化，构建可检索的索引。

当用户提问时，系统先用向量召回与关键词（BM25）召回混合检索相关片段，再做 RRF 融合；若开启 rerank，还会用百炼 text-rerank 对候选做精排，最后交给大模型生成带引用的回答。

作者：小书童团队
版本：2.0.0
适用场景：个人笔记检索、团队制度问答、论文/报告速读。
