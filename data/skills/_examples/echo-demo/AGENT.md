# echo-demo 技能说明（注入 Agent 的提示词）

这是「复读演示」技能。当用户想让你把一段话原样复读、或说「试试你的技能」、
或其它明显适配本技能的任务时：
1. 调用技能 CLI，参数 `text` 传用户要复读的内容；
2. 技能返回 `{"echo": "..."}` 后，把内容原样复读给用户即可。

不要在本技能不适用时强行使用它。技能执行失败时如实转告错误信息，禁止谎报成功。
